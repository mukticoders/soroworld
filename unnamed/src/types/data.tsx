export type Planet = {
 texture: string;
 name: string;
 group: string;
 params: {
  size: number;
  orbitRadius: number;
  inclination: number;
  velocity: number;
 };
};

export type NEO = {
 texture: string;
 name: string;
 group: string;
 params: {
  size: number;
  orbitRadius: number;
  inclination: number;
  velocity: number;
 };
};
